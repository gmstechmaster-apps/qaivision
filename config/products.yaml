# Product under test, configurable per environment and site (spec section 10).
# NLP scenarios refer to "the configured product" — the Planner Agent resolves
# the actual product name from here at runtime.

products:
  dev:
    example-site:
      product: CHANGE_ME
  stg:
    example-site:
      product: CHANGE_ME
  prd:
    example-site:
      product: CHANGE_ME
