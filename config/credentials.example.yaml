# Template for config/credentials.yaml — copy this file to credentials.yaml
# and fill in real values. credentials.yaml is gitignored and must never be
# committed or referenced from an .nlp file (spec section 11).

credentials:
  dev:
    example-site:
      username: CHANGE_ME
      password: CHANGE_ME
  stg:
    example-site:
      username: CHANGE_ME
      password: CHANGE_ME
  prd:
    example-site:
      username: CHANGE_ME
      password: CHANGE_ME
